# ... (O cabeçalho e 'jobs' permanecem iguais)
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Instalar dependências  # NOVO STEP 1
        run: pip install pytest

      - name: Executar testes  # NOVO STEP 2
        run: pytest -v
        
      - name: Executar script
        run: python main.py